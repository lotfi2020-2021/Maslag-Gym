{% extends 'base3.html.twig' %}
{% block title %}Product{% endblock %}
{% block body %}
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8">

            </div>
            <div class="col-md-4">
                <h1>{{ product.NomP }}</h1>
                <div class="text primary" style="font-size: 4rem; font-weight: bold;">{{ product.PrixP }} D</div>
                <a href="{{ path('contactp') }}" class="btn btn-primary">contact</a>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-8">
            <h2>carectéristique</h2>
            <table class="table table-striped">
                <tbody>

                <tr>
                    <th>Marque_p</th>
                    <td>{{ product.MarqueP }}</td>
                </tr>
                <tr>
                    <th>image</th>
                    <td><img src="{{vich_uploader_asset(product, 'imageFile')}}" ></td>
                </tr>
                <tr>
                    <th>categorie</th>
                    <td>{{ product.categorie }}</td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>
    <body/>
{% endblock %}
