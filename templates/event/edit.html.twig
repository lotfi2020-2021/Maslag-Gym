{% extends 'base2.html.twig' %}

{% block title %}Edit Evenement{% endblock %}

{% block body %}
    <h1>Edit Evenement</h1>
    {{ include('event/_form.html.twig', {'button_label': 'Update'}) }}
    <button class="btn">
    <a class="fas fa-list" style="color: #FFFFFF" href="{{ path('event_index') }}">lista</a>
    </button>
    {{ include('event/_delete_form.html.twig') }}
{% endblock %}
