{% extends 'base.html.twig' %}

{% block title %}Category2{% endblock %}

{% block body %}
    <h1>Category2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category2.id }}</td>
            </tr>
            <tr>
                <th>Labell1</th>
                <td>{{ category2.labell1 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cat_c_index') }}">back to list</a>

    <a href="{{ path('cat_c_edit', {'id': category2.id}) }}">edit</a>

    {{ include('cat_c/_delete_form.html.twig') }}
{% endblock %}
