<form method="post" action="{{ path('cat_c_delete', {'id': category2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category2.id) }}">
    <button class="btn">Delete</button>
</form>
