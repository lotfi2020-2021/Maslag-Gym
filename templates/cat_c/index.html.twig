{% extends 'base.html.twig' %}

{% block title %}Category2 index{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Labell1</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category2 in category2s %}
            <tr>
                <td>{{ category2.id }}</td>
                <td>{{ category2.labell1 }}</td>
                <td>
                    <a href="{{ path('cat_c_show', {'id': category2.id}) }}">show</a>
                    <a href="{{ path('cat_c_edit', {'id': category2.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('cat_c_new') }}">Create new</a>
{% endblock %}
