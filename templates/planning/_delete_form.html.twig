<form method="post" action="{{ path('planning_delete', {'id': planning.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planning.id) }}">
    <button class="fa fa-trash" style="background: #1e1e2d;font-size:50px;color: #ba54f5;margin-left: 25%;border:0px"></button>
</form>
