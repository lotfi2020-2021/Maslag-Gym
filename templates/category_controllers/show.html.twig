{% extends 'base2.html.twig' %}
{% block title %}Category{% endblock %}
{% block body %}
<body style="color: #c69500">
    <h1>Category</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ category.label }}</td>
            </tr>
        </tbody>
    </table>
</body>
    <a class="fa fa-home " style="font-size:48px;color:#ba54f5;margin-left: 15%"  href="{{ path('category_controllers_index') }}"></a>
    <a class="fa fa-edit" href="{{ path('category_controllers_edit', {'id': category.id}) }}"></a>
    {{ include('category_controllers/_delete_form.html.twig') }}

{% endblock %}
