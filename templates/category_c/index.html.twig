{% extends 'base2.html.twig' %}

{% block title %}Category index{% endblock %}

{% block body %}
    <div style="margin-top:2%;">
        <table>
            <tr ><a style="margin-left:40%;color: #c69500;font-size:30px" href="{{ path('category_c_index') }}">categories</a> </tr>
            <tr> <a style="margin-left:3%; color: #c69500;font-size: 30px" href="{{ path('planning_index') }}">Planning</a></tr>
            <tr> <a style="margin-left:3%; color: #c69500;font-size: 30px" href="{{ path('main') }}">calendrier</a></tr>
            <tr> <a style="margin-left:3%; color: #c69500;font-size: 30px" href="{{ path('contactC') }}">contact</a></tr>
            <tr> <a style="margin-left:3%; color: #c69500;font-size: 30px" href="{{ path('stats') }}">statistiques</a></tr>
        </table>
    </div>
    <h1 style="width:50%;margin:auto">
        <span style="color:#66CC66">C</span>
        <sub>  <span style="color: #3f51b5">A</span></sub>
        <span style="color:#FFCCCC">T</span>
        <sub>  <span style="color:#FF0066">E</span></sub>
        <span style="color:#FF0000">G</span>
        <sub>   <span style="color:#66CC66">O</span></sub>
        <span style="color:#3f51b5">R</span>
        <sub>  <span style="color:#FFCCCC">Y</span></sub>
    </h1>

    <table class="table table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Category</th>
                <th>actions</th>
                <th>Colors</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.labell }}</td>
                <td>{{ category.color }}</td>
                <td>
                    <a style="font-size: 40px"   class=" fa fa-bars" href="{{ path('category_c_show', {'id': category.id}) }}"></a>
                </td>
                <td>
                    <a class=" fa fa-edit"  style="font-size: 40px" href="{{ path('category_c_edit', {'id': category.id}) }}"></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class=" fa fa-plus" href="{{ path('category_c_new') }}" style="font-size: 40px"> Ajouter</a>
{% endblock %}
